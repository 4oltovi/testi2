<?php

/**
 * Танзимоти махсуси системаи Донишёр
 */

return [

    /*
    |--------------------------------------------------------------------------
    | Танзимоти баҳогузорӣ
    |--------------------------------------------------------------------------
    */
    'grading' => [
        // Формулаи баҳои ниҳоӣ
        // 'with_independent_work' = R1×0.15 + R2×0.15 + КМ×0.30 + Exam×0.40
        // 'without_independent_work' = R1×0.30 + R2×0.30 + Exam×0.40
        'formula_type' => env('GRADE_FORMULA_TYPE', 'with_independent_work'),

        'weights' => [
            'with_iw' => [
                'rating1' => 0.15,
                'rating2' => 0.15,
                'independent_work' => 0.30,
                'exam' => 0.40,
            ],
            'without_iw' => [
                'rating1' => 0.30,
                'rating2' => 0.30,
                'exam' => 0.40,
            ],
        ],

        // Рейтингҳо: ҳафтаҳо
        'rating1_weeks' => [1, 8],   // Рейтинги 1: ҳафтаи 1-8
        'rating2_weeks' => [9, 16],  // Рейтинги 2: ҳафтаи 9-16

        // Ҳадди ақали давомот барои иҷозат ба имтиҳон
        'min_attendance_percentage' => (int) env('ATTENDANCE_MIN_PERCENTAGE', 75),
    ],

    /*
    |--------------------------------------------------------------------------
    | Танзимоти қарздорӣ
    |--------------------------------------------------------------------------
    */
    'debts' => [
        // Шумораи ҳадди аксари такрорсупорӣ
        'max_retake_attempts' => (int) env('MAX_RETAKE_ATTEMPTS', 2),

        // Fx = имкони такрорсупорӣ, F = дубора хондан
        'fx_allows_retake' => true,
        'f_requires_repeat' => true,
    ],

    /*
    |--------------------------------------------------------------------------
    | Танзимоти имтиҳон
    |--------------------------------------------------------------------------
    */
    'exam' => [
        'max_duration_minutes' => (int) env('EXAM_MAX_DURATION_MINUTES', 120),
        'auto_save_interval_seconds' => (int) env('EXAM_AUTO_SAVE_INTERVAL', 30),
        'default_questions_count' => 30,
        'default_passing_score' => 50.0,
        'max_disconnection_count' => 5,
    ],

    /*
    |--------------------------------------------------------------------------
    | Танзимоти GPA ва Transcript
    |--------------------------------------------------------------------------
    */
    'gpa' => [
        // Honors categories
        'summa_cum_laude_min' => 3.80,  // Бо ифтихори аъло
        'magna_cum_laude_min' => 3.50,  // Бо ифтихор
        'cum_laude_min' => 3.00,        // Қобили таваҷҷуҳ
    ],

    /*
    |--------------------------------------------------------------------------
    | Танзимоти пагинатсия
    |--------------------------------------------------------------------------
    */
    'pagination' => [
        'per_page' => (int) env('PAGINATION_PER_PAGE', 25),
    ],

    /*
    |--------------------------------------------------------------------------
    | Танзимоти audit log
    |--------------------------------------------------------------------------
    */
    'audit' => [
        'enabled' => true,
        // Пок кардани логҳои кӯҳнатар аз (рӯз)
        'retention_days' => 365,
    ],
];
